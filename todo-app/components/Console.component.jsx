// 'use client';

// // mounts a console component to the bottom of the page when the user presses the : key
// import React, {useState} from 'react'

// const Console = () => {
//     const [ isVisible, setIsVisible ] = useState( false )

//     const handleConsoleActivate = ( e ) => {
//         // when activated:
//         // 1. the console should be visible
//         // 2. the console should be focused
//         // 3. the console should be scrollable
//         // 4. the console should have a close button, or close when the user presses the escape key

//         // if the console is already visible, hide it
//         if ( isVisible ) {
//             setIsVisible( false )
//             return
//         } else {
//             setIsVisible( true )
//         }

//         // focus the console

//         // make the console scrollable

//         // add a close button to the console
//   return (
//     <div>Console</div>
//   )
// }

// export default Console
